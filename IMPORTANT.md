# ⚠️ 重要：重新加载插件后需要刷新页面

## 问题说明

Chrome扩展有一个已知限制：当你重新加载（reload）扩展后，已经打开的页面中的content script **不会自动更新**。

## 解决方案

重新加载插件后，你需要：

### 方案1：刷新ChatGPT页面（推荐）
1. 在 `chrome://extensions/` 页面点击"重新加载"按钮
2. 回到 **ChatGPT页面**
3. **按 F5 或 Ctrl+R 刷新页面**
4. 插件将重新初始化

### 方案2：完全重新安装
1. 在 `chrome://extensions/` 页面点击"移除"按钮
2. 点击"加载已解压的扩展程序"
3. 选择插件文件夹
4. 打开ChatGPT页面

## 对话切换功能

插件现在支持自动检测对话切换并重置消息列表：

- ✅ 从对话A切换到对话B时，消息列表会自动清空
- ✅ 每个对话的消息独立保存
- ✅ 搜索框在切换对话时自动清空

## 测试步骤

1. 刷新ChatGPT页面后，打开第一个对话
2. 观察侧边栏显示的消息数量（例如：2条）
3. 点击左侧边栏切换到另一个对话
4. 观察侧边栏的消息数量是否更新为新对话的数量

## 已知问题

- 首次加载插件后，旧对话可能会显示历史缓存消息
- 切换对话时有1秒延迟检测URL变化
- 刷新页面是应用新代码的唯一可靠方法
